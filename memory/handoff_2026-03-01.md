# EasternLM Handoff 2026-03-01

## Current Deployment State

- VPS: `root@5.161.88.134` (`hampton-vps` SSH alias)
- Git bare repo on VPS: `/opt/repos/easternlm-web.git`
- Clone on VPS: `/opt/easternlm-web`
- Running container: `easternlm-web`
- Public test URL (direct): `http://5.161.88.134:3100`
- Staging host routing at nginx: `staging.easternlm.com` -> `easternlm-web:3000`

## DNS and Staging

- `staging.easternlm.com` now resolves to `5.161.88.134`.
- HTTP on staging host is working through nginx.
- HTTPS now uses a publicly trusted Let's Encrypt certificate.

## SSL (Completed 2026-03-01)

- Installed Let's Encrypt cert for `staging.easternlm.com`:
  - `/etc/letsencrypt/live/staging.easternlm.com/fullchain.pem`
  - `/etc/letsencrypt/live/staging.easternlm.com/privkey.pem`
- Updated VPS proxy stack (`/opt/hosthampton`):
  - Added nginx challenge path for `/.well-known/acme-challenge/` on port 80.
  - Added Docker mounts for nginx container:
    - `/etc/letsencrypt:/etc/letsencrypt:ro`
    - `/var/www/certbot:/var/www/certbot:ro`
  - Updated staging HTTPS vhost cert paths to Let's Encrypt files.
- Added renewal cron:
  - `/etc/cron.d/certbot-staging-easternlm`
  - Runs at `03:17` and `15:17` daily and reloads nginx on renewal.
- Certificate validity:
  - `notBefore=Mar 1 16:12:13 2026 GMT`
  - `notAfter=May 30 16:12:12 2026 GMT`

## Todo

1. Verify renewal logs after first scheduled run and keep cert automation healthy.
2. Keep app updates via Git flow:
   - local repo push -> `/opt/repos/easternlm-web.git`
   - VPS pull in `/opt/easternlm-web`
   - rebuild/restart `easternlm-web` container
3. Run Supabase migration + seed workflow when ready (deferred by user):
   - apply migrations in `supabase/migrations`
   - run `supabase/seed.sql`

## Notes

- A local Dockerfile and `.dockerignore` were added for reproducible VPS deployment.
- The app builds with SSG routes, including `/delivery/[town]` pages.
