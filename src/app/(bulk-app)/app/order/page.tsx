/**
 * /app/order — Order Summary, Delivery, & Payment
 * Spec: Section 9 (order UX), Section 5 (delivery), Section 7 (payment)
 * Phase 03 will implement the full order flow.
 */
export default function BulkOrderPage() {
  return (
    <div className="flex min-h-screen items-center justify-center px-6">
      <p className="text-sm text-bulk-muted">Order Summary — Coming Soon</p>
    </div>
  );
}
