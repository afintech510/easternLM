// Spec: Section 5 — Address + date picker + drop instructions + access constraints
"use client";
export function DeliveryForm() {
  // TODO: implement — address input (Google Places), date pills, instructions textarea, access checkboxes
  return <div data-testid="delivery-form">DeliveryForm</div>;
}
