// Spec: Section 9 — Floating bottom bar (items count, cu yds, sq ft, total, CTA)
"use client";
export function OrderBar() {
  // TODO: implement — fixed bottom bar, dark green gradient, shows when items > 0
  return <div data-testid="order-bar">OrderBar</div>;
}
